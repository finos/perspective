
dist: trusty

language: node_js

node_js:
  - "8"

env:
  global:
    - EM_USE_GLOBAL_CACHE=1

sudo: required

services:
  - docker

install:
  - npm install

script:
  - npm run docker_build
  - npm run compile_test
  - npm run docker_test
